version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused
  settings:
    govet:
      enable-all: true

formatters:
  enable:
    - gofmt
    - goimports

run:
  tests: true
